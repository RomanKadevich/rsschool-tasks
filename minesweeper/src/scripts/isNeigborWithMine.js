// export function isNeigborWithMine(row, col, itemFieldObj) {
//     const lengthRows = field.length;
//     const lengthCols = field.length;
//     const firstRow = Math.max(0, row - 1);
//     const lastRow = Math.min(row + 1, lengthRows - 1);
//     const firstCol = Math.max(0, row - 1);
//     const lastCol = Math.min(col + 1, lengthCols - 1);
//     let counterMines = 0;
//     for (let i = firstRow; i <= lastRow; i++){
//         for (let j = firstCol; j <= lastCol; i++){
//             if (i === row && j === col){
//                 continue
//             }
//             if (itemFieldObj.withMine = true){
//                 counterMines++;
//             }
//         }
//     }
//     return counterMines;
// }
